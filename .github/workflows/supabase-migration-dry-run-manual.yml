name: Supabase migration dry-run

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  dry-run:
    runs-on: ubuntu-latest

    steps:
      - name: Parser test
        run: echo "dry-run workflow parser OK"
