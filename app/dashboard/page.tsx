import { AppShell } from '@/components/app-shell'; import { StatCard } from '@/components/stat-card'; import { getCurrentUser } from '@/services/auth'; import { listUserNumbers } from '@/repositories/numbers'; import { listMessages } from '@/repositories/messages';
export default async function DashboardPage(){const user=await getCurrentUser(); if(!user) return <AppShell><div className="glass rounded-2xl p-8"><h1 className="text-2xl font-black">Dashboard</h1><p className="mt-2 muted">Silakan login untuk melihat workspace.</p></div></AppShell>; const [numbers,messages]=await Promise.all([listUserNumbers(user.id),listMessages(user.id)]); return <AppShell><div className="mb-6"><div className="text-sm muted">Workspace</div><h1 className="text-3xl font-black">Dashboard</h1><p className="mt-1 text-sm muted">{user.email}</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Active Numbers" value={String(numbers.filter((n:any)=>n.status==='active').length)} detail="DID aktif"/><StatCard label="Total Numbers" value={String(numbers.length)} detail="Dalam akun"/><StatCard label="Unread / recent" value={String(messages.length)} detail="Pesan inbound"/><StatCard label="API" value="Ready" detail="Server-side route handlers"/></div><div className="glass mt-5 rounded-2xl p-6"><h2 className="font-bold">System status</h2><div className="mt-4 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-white/[.03] p-4"><b>Supabase</b><div className="mt-1 text-xs muted">Auth • DB • Realtime</div></div><div className="rounded-xl bg-white/[.03] p-4"><b>Providers</b><div className="mt-1 text-xs muted">Adapter architecture</div></div><div className="rounded-xl bg-white/[.03] p-4"><b>Security</b><div className="mt-1 text-xs muted">RLS + server secrets</div></div></div></div></AppShell>;}
