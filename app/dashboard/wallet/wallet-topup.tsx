'use client';
import {useState} from 'react';
export function WalletTopup(){const [amount,setAmount]=useState('50000'),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function submit(){setBusy(true);setMsg('');const r=await fetch('/api/wallet/deposit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount_cents:Math.round(Number(amount)),idempotency_key:crypto.randomUUID()})});const b=await r.json();setBusy(false);if(r.ok&&b.data?.checkout_url){location.href=b.data.checkout_url;return}if(r.ok){setMsg('Deposit completed.');location.reload();}else setMsg(b.error?.message??'Deposit failed')}return <div className="mt-6"><input className="field" inputMode="numeric" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="50000"/><button className="btn btn-primary mt-3 w-full" disabled={busy} onClick={submit}>{busy?'Processing…':'Deposit'}</button>{msg&&<div className="mt-3 text-sm muted">{msg}</div>}</div>}
