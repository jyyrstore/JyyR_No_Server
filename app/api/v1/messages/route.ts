import { NextResponse } from 'next/server'; import { authenticateRequest } from '@/services/request-auth'; import { createAdminClient } from '@/lib/supabase-admin';
export async function GET(request:Request){const a=await authenticateRequest(request,'messages:read');if(!a.ok)return NextResponse.json({error:a.error},{status:a.status});const {data,error}=await createAdminClient().from('inbound_messages').select('*').eq('user_id',a.userId).order('received_at',{ascending:false}).limit(100);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({data:data??[]});}
