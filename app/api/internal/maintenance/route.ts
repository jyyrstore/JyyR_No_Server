import { NextResponse } from 'next/server'; import { serverEnv } from '@/lib/env'; import { dispatchDueWebhookDeliveries } from '@/services/webhook-delivery';
export async function GET(request:Request){const secret=serverEnv().CRON_SECRET;if(!secret||request.headers.get('authorization')!==`Bearer ${secret}`)return NextResponse.json({error:'Unauthorized'},{status:401});try{const processed=await dispatchDueWebhookDeliveries(50);return NextResponse.json({ok:true,processed,warning:'Use an external scheduler for sub-hour retry cadence on Vercel Hobby.'});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:'Maintenance failed'},{status:500});}}
