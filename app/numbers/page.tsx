import { AppShell } from '@/components/app-shell'; import { getCurrentUser } from '@/services/auth'; import { listUserNumbers } from '@/repositories/numbers';
export default async function Numbers(){const user=await getCurrentUser(); if(!user)return <AppShell><div className="glass rounded-2xl p-6">Login required.</div></AppShell>; const rows=await listUserNumbers(user.id); return <AppShell><div className="flex items-end justify-between"><div><h1 className="text-3xl font-black">Numbers</h1><p className="mt-1 text-sm muted">DID yang dimiliki akun.</p></div></div><div className="glass mt-5 overflow-hidden rounded-2xl"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-white/5 muted"><tr><th className="p-4">Number</th><th className="p-4">Country</th><th className="p-4">Status</th><th className="p-4">Monthly</th></tr></thead><tbody>{rows.map((n:any)=><tr key={n.id} className="border-b border-white/5"><td className="p-4 font-semibold">{n.phone_number}</td><td className="p-4">{n.country_code}</td><td className="p-4">{n.status}</td><td className="p-4">{(Number(n.monthly_price_cents) / 100).toFixed(2)}</td></tr>)}{rows.length===0&&<tr><td colSpan={4} className="p-8 text-center muted">Belum ada number.</td></tr>}</tbody></table></div></div></AppShell>}
