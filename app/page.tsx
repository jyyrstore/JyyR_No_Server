import Link from 'next/link';
import {Navbar} from '@/components/navbar';
export default function Home(){return <div><Navbar/><main className="mx-auto max-w-6xl px-5 py-16 sm:py-24"><div className="max-w-4xl"><div className="mb-5 inline-flex rounded-full border border-purple-400/20 bg-purple-400/10 px-3 py-1 text-xs font-bold text-purple-200">Jyy&apos;R Virtual Numbers</div><h1 className="text-5xl font-black leading-tight sm:text-7xl">Buy a temporary number.<br/><span className="text-purple-400">Receive the OTP.</span></h1><p className="mt-6 max-w-2xl text-lg muted">One-time virtual numbers for legitimate SMS verification workflows, with transparent pricing, live order status, wallet ledger, and provider-backed delivery.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn btn-primary" href="/auth/register">Create account</Link><Link className="btn btn-ghost" href="/auth/login">Sign in</Link></div></div><div className="mt-16 grid gap-4 md:grid-cols-3"><div className="glass rounded-2xl p-6"><div className="text-2xl">⚡</div><h2 className="mt-3 font-bold">Fast purchase</h2><p className="mt-2 text-sm muted">Country → service → price → number, with server-authoritative stock and idempotent orders.</p></div><div className="glass rounded-2xl p-6"><div className="text-2xl">💳</div><h2 className="mt-3 font-bold">Auditable wallet</h2><p className="mt-2 text-sm muted">Deposits, OTP purchases, refunds, and adjustments are recorded in a dedicated ledger.</p></div><div className="glass rounded-2xl p-6"><div className="text-2xl">📩</div><h2 className="mt-3 font-bold">Realtime OTP</h2><p className="mt-2 text-sm muted">Validated provider events update the active order without a manual refresh.</p></div></div><section className="mt-16 glass rounded-3xl p-8"><h2 className="text-2xl font-black">How it works</h2><div className="mt-6 grid gap-6 sm:grid-cols-4">{['Register / Login','Deposit balance','Choose country + service','Buy and receive SMS'].map((x,i)=><div key={x}><div className="text-sm font-black text-purple-300">0{i+1}</div><div className="mt-2 font-bold">{x}</div></div>)}</div></section></main></div>}
