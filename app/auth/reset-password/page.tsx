'use client';
import {useState} from 'react'; import {createClient} from '@/lib/supabase-browser';
export default function Reset(){const[pwd,setPwd]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const {error}=await createClient().auth.updateUser({password:pwd});if(error)setErr(error.message);else setMsg('Password updated. You can sign in again.')}return <main className="grid min-h-screen place-items-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7"><h1 className="text-2xl font-black">Choose a new password</h1><form className="mt-6 space-y-3" onSubmit={submit}><input className="field" type="password" minLength={8} required value={pwd} onChange={e=>setPwd(e.target.value)} placeholder="New password"/><button className="btn btn-primary w-full">Update password</button></form>{msg&&<p className="mt-4 text-sm text-emerald-200">{msg}</p>}{err&&<p className="mt-4 text-sm text-red-200">{err}</p>}</div></main>}
