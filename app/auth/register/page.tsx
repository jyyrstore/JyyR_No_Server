'use client';
import {useState} from 'react'; import Link from 'next/link'; import {createClient} from '@/lib/supabase-browser';
export default function Register(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setErr('');setMsg('');const s=createClient();const {data,error}=await s.auth.signUp({email,password,options:{data:{display_name:name},emailRedirectTo:`${location.origin}/auth/callback`}});if(error){setErr(error.message);return}setMsg(data.session?'Account created.':'Account created. Check your email to verify it.')}return <main className="grid min-h-screen place-items-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7"><h1 className="text-2xl font-black">Create account</h1><p className="mt-2 text-sm muted">Use an email you control and verify it before buying.</p><form className="mt-6 space-y-3" onSubmit={submit}><input className="field" placeholder="Display name" value={name} onChange={e=>setName(e.target.value)}/><input className="field" type="email" required placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><input className="field" type="password" required minLength={8} placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary w-full">Register</button></form>{msg&&<div className="mt-4 rounded-xl bg-emerald-500/10 p-4 text-sm text-emerald-200">{msg}</div>}{err&&<div className="mt-4 rounded-xl bg-red-500/10 p-4 text-sm text-red-200">{err}</div>}<div className="mt-5 text-center text-sm muted"><Link className="text-purple-300" href="/auth/login">Already have an account?</Link></div></div></main>}
