'use client';
import {useState} from 'react'; import {createClient} from '@/lib/supabase-browser'; import Link from 'next/link';
export default function Forgot(){const[email,setEmail]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth/reset-password`});if(error)setErr(error.message);else setMsg('Reset email sent.')}return <main className="grid min-h-screen place-items-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7"><h1 className="text-2xl font-black">Reset password</h1><form className="mt-6 space-y-3" onSubmit={submit}><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><button className="btn btn-primary w-full">Send reset link</button></form>{msg&&<p className="mt-4 text-sm text-emerald-200">{msg}</p>}{err&&<p className="mt-4 text-sm text-red-200">{err}</p>}<Link className="mt-5 block text-center text-sm text-purple-300" href="/auth/login">Back to login</Link></div></main>}
