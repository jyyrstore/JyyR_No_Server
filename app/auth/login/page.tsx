'use client';
import { useState } from 'react';
import { createClient } from '@/lib/supabase-browser';
export default function LoginPage() { const [email,setEmail]=useState(''); const [sent,setSent]=useState(false); const [error,setError]=useState(''); async function submit(e:React.FormEvent){e.preventDefault();setError('');const supabase=createClient();const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});if(error)setError(error.message);else setSent(true);} return <main className="grid min-h-screen place-items-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7"><h1 className="text-2xl font-black">Masuk ke Number Server</h1><p className="mt-2 text-sm muted">Magic link akan dikirim ke email kamu.</p>{sent?<div className="mt-6 rounded-xl border border-purple-400/20 bg-purple-400/10 p-4 text-sm">Link login sudah dikirim. Cek inbox email.</div>:<form onSubmit={submit} className="mt-6 space-y-4"><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><button className="btn btn-primary w-full">Kirim Magic Link</button>{error&&<p className="text-sm text-red-300">{error}</p>}</form>}</div></main>; }
