import { AppShell } from '@/components/app-shell'; import { getCurrentUser } from '@/services/auth'; import { createServerSupabaseClient } from '@/lib/supabase-server'; import { WebhookManager } from './webhook-manager';
export default async function Webhooks(){const user=await getCurrentUser();if(!user)return <AppShell><div className="glass rounded-2xl p-8">Login required.</div></AppShell>;const s=await createServerSupabaseClient();const {data}=await s.from('webhooks').select('id,endpoint_url,event,status,retry_policy,last_delivery_at,created_at').eq('user_id',user.id).order('created_at',{ascending:false});return <AppShell><h1 className="text-3xl font-black">Webhooks</h1><p className="mt-1 text-sm muted">Signed, retried, observable event delivery.</p><div className="mt-6"><WebhookManager initial={data??[]}/></div></AppShell>}
