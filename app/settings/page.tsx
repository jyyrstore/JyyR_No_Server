import { AppShell } from '@/components/app-shell'; import { getCurrentUser } from '@/services/auth';
export default async function Settings(){const user=await getCurrentUser(); return <AppShell><h1 className="text-3xl font-black">Settings</h1><div className="glass mt-5 max-w-2xl rounded-2xl p-6"><div className="text-sm muted">Authenticated email</div><div className="mt-2 font-semibold">{user?.email ?? 'Not signed in'}</div><div className="mt-6 text-sm muted">Security</div><p className="mt-2 text-sm">Provider secrets, service-role keys, database credentials, dan webhook signing secrets harus tetap server-side.</p></div></AppShell>}
