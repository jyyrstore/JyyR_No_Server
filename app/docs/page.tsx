import { AppShell } from '@/components/app-shell';
export default function Docs(){return <AppShell><h1 className="text-3xl font-black">API Docs</h1><div className="mt-5 grid gap-4 lg:grid-cols-2"><div className="glass rounded-2xl p-6"><div className="font-bold">Authentication</div><p className="mt-2 text-sm muted">Gunakan API key server-side melalui header <code className="text-purple-300">x-api-key</code>. Secret hanya ditampilkan sekali saat dibuat.</p></div><div className="glass rounded-2xl p-6"><div className="font-bold">Inbound SMS</div><p className="mt-2 text-sm muted">Pesan inbound tersedia melalui endpoint messages setelah provider resmi terhubung.</p></div></div><pre className="glass mt-4 overflow-auto rounded-2xl p-6 text-sm text-purple-100">{`GET  /api/v1/providers\nGET  /api/v1/numbers       (numbers:read)\nGET  /api/v1/messages      (messages:read)\nGET  /api/v1/webhooks      (webhooks:read)\nPOST /api/v1/webhooks      (webhooks:write)\n\nHeader:\nx-api-key: your_secret_key`}</pre></AppShell>}
