import { AppShell } from '@/components/app-shell'; import { getCurrentUser } from '@/services/auth'; import { createServerSupabaseClient } from '@/lib/supabase-server';
export default async function Admin(){const user=await getCurrentUser(); if(!user)return <AppShell><div className="glass rounded-2xl p-6">Login required.</div></AppShell>; const supabase=await createServerSupabaseClient(); const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).single(); if(!profile||profile.role!=='admin')return <AppShell><div className="glass rounded-2xl p-6"><h1 className="text-xl font-black">Admin area</h1><p className="mt-2 muted">Akses admin diperlukan.</p></div></AppShell>; return <AppShell><h1 className="text-3xl font-black">Admin</h1><div className="glass mt-5 rounded-2xl p-6">Admin tools siap dihubungkan ke server-side management actions.</div></AppShell>}
