import { AppShell } from '@/components/app-shell'; import { requireAdmin } from '@/services/admin'; import { createAdminClient } from '@/lib/supabase-admin';
export default async function Admin(){const user=await requireAdmin();if(!user)return <AppShell><div className="glass rounded-2xl p-8"><h1 className="text-2xl font-bold">Admin access required</h1></div></AppShell>;const db=createAdminClient();const [{count:users},{count:numbers},{count:orders},{count:failedWebhooks}]=await Promise.all([db.from('profiles').select('*',{count:'exact',head:true}),db.from('phone_numbers').select('*',{count:'exact',head:true}),db.from('number_orders').select('*',{count:'exact',head:true}),db.from('webhook_deliveries').select('*',{count:'exact',head:true}).eq('status','failed')]);return <AppShell><div className="flex items-end justify-between"><div><p className="text-sm muted">Control plane</p><h1 className="text-3xl font-black">Admin</h1></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Users',users],['Numbers',numbers],['Orders',orders],['Failed webhooks',failedWebhooks]].map(([label,value])=><div key={String(label)} className="glass rounded-2xl p-5"><div className="text-sm muted">{label}</div><div className="mt-2 text-3xl font-black">{value??0}</div></div>)}</div></AppShell>}
